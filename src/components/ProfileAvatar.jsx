import React from "react";
const ProfileAvatar = ({ image }) => {
  return (
    <img
      src={image}
      alt="profile"
      width="100"
      height="100"
     
    />
  );
};

export default ProfileAvatar;
